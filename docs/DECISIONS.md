# 의사결정 문서 (DECISIONS.md)

> 목적: **구현 전에** 선생님이 결정하실 것을 모아둡니다. 각 항목은
> `배경/관찰 → 선택지(장단점) → 제 추천 → 결정 시 제가 할 일 → 검증 방법` 순입니다.
> 읽어보시고, 맨 아래 **결정 양식**처럼 골라주시면 그대로 진행합니다.
> 원칙(CLAUDE.md): 범위는 미리 합의 · 최소·외과적 변경 · 실제로 검증.
>
> 작성: 2026-06-26 · 관련: [`PLAN.md`](./PLAN.md) · [`LOGGING.md`](./LOGGING.md) · [`ARCHITECTURE.md`](./ARCHITECTURE.md)

---

## 확정된 결정 (2026-07-02)

이 세션에서 사용자 승인 후 실행 완료한 결정들.

- **D6. 폴더 재구성** — 설계·기록 `.md`는 `docs/`로, 사람 큐레이션·검수 입력(`external_context.json`·`mapping_review.csv`)은 "설정이 아니라 참조 데이터"이므로 `config/`가 아닌 **`curation/`**로. (README/CLAUDE는 루트 유지.)
- **D7. `rag/` 서브패키지화** — flat 25파일 → `core·ingest·transform·curate·retrieval` 서브패키지 + 최상위 `signals·pipeline`. 실행모델을 파일 경로 → **`python -m rag.<pkg>.<mod>`**로 전환, dual-import(try/except) 제거·단일 절대 import. *(엔지니어 권고는 flat 유지였으나 사용자가 전면 재편 선택 — CLI 계약 변경 감수.)*
- **D8. 로거 표준화** — 모듈 로거명을 `log`→**`logger`**로 통일. 리터는 파일별 수정이 아니라 **설정**(`pyproject [tool.ruff]`·`cspell.json`)으로.
- **D9. 정합성 결함 6건 수정** — "추측은 데이터가 아니다" 불변식을 코드로 강제: 비전 old-table을 medium+warning으로 검수 큐에 태우고, integrate side-channel을 `validate` 새 검사(#5)로 차단. 나머지 4건(연도 오인·index None·출처 페이지·dedup 드롭)도 재현 테스트와 함께 수정.
- **D10. 모델 유지** — `config.py`의 `gpt-5.4-mini`·`text-embedding-3-small`는 models API 실측상 최신 근접·정상 작동으로 **교체하지 않음**(정밀도 필요 시 추출/비전만 상향은 열린 옵션).

---

## 확정된 결정 (2026-07-03)

이 세션에서 사용자 승인 후 실행 완료한 결정들.

- **D11. 방법론 지식 인덱싱** — 큐레이션된 '비교 유의' 지식(`curation/methodology_notes.json`, 척도 변경 등)을 `parser_type="methodology"` 지식청크로 만들어 **사실 청크와 함께 인덱싱**(현재 사실 196 + 방법론 6 + 외부 맥락 16 = 218 청크). 준비 게이트는 사실 청크만 검사해 지식청크를 '빈 청크'로 오인하지 않음. *불변식 진화*: "추측은 데이터가 아니다" → **사람이 출처와 함께 확정한 해석 지식은 종류(parser_type)가 분명하면 인덱싱 대상**. `rag/curate/methodology.py`가 청킹·앱 캡션의 단일 소스(드리프트 방지).
- **D12. 신호등 의사결정 프레이밍** — 헤드라인 "📊 주목할 실제 변화"는 **설문 설계가 동일한 비교 가능 전이(2024→2025)만**. 2023→2024 설문 개편 구간·문서상 척도 변경은 "⚠️ 해석 유의"로 분리(값은 보여주되 실제 추세로 헤드라인하지 않음). 근거: 데이터상 2023→2024 급변이 2024→2025보다 많음(부분 개편의 흔적). `signals.py`가 집계·비응답 라벨 제외·척도경계 보류(`caveat_break`)·이진 상보 중복 제거(`is_binary_mirror`)를 순수 함수로 강제. 추세 차트 연도 축 quantitative→ordinal(중복 눈금·가짜 보간·라벨 잘림 해소).
- **D13. `app.py` → `ui/` 모듈화(완료)** — 비대해진 `app.py`(1935행)에서 단계·탭 화면을 `ui/` 패키지로 전량 추출: `ui/signal.py`(895행, 6단계 신호등) · `ui/review.py`(426행, 5단계 검수) · `ui/ingest.py`(355행, 1·2단계 업로드·인제스트) · `ui/rag.py`(107행, 5단계 질의) · `ui/index.py`(56행, 4단계 인덱싱) · `ui/common.py`(18행, 공유 상수·경로). `app.py`는 **309행**(진입·라우팅·가이드 스텝퍼·상태/로그 패널 셸 + `main()`)만 남음. 한 모듈씩 추출·검증(ruff 0 · 전체 테스트 68 passed, 신규 실패 0)으로 동작 보존.
- **D14. 외부 맥락 RAG 통합(변곡점 해석)** — '변곡점 × 외부 맥락'이 대시보드 패널에서만 쓰이고 RAG와 단절돼, advise 모드가 사건을 몰라 **해석이 상황에 맞게 달라지지 않던** 문제 해소. `curation/external_context.json`(그해 뉴스·사건)을 `parser_type="external_context"` 지식청크(16건)로 인덱싱(D11 패턴 재사용), `_advise_retrieve`에 외부 맥락 facet 추가, 프롬프트가 **데이터 변화 × 그해 사건을 엮은 상황 해석**을 생성하되 **상관·인과를 구분**하도록 강제. 실 LLM 검증: "2023→2024 신뢰 상승 ↔ 그린워싱 이슈 확대(상관·인과 아님)"를 근거·출처와 함께 생성. 대시보드 패널은 겹치는 연도(대조 가능)와 설문 이전(배경 맥락)을 분리해 '데이터 없음' 노이즈 제거. *남은 근본 제약(정직하게 명시)*: 설문 데이터가 2023~2025 3개년(깨끗한 전이는 2024→2025 하나)이라 해석의 천장은 낮음 — 2014~2022 옛 보고서 인제스트가 선결 과제(PLAN 백로그 D).
- **D15. 클론 즉시 재현 레퍼런스 정책(하이브리드 · Option A)** — "공개 official 보고서라 누구나 클론 후 바로 본다"를 위해 레퍼런스 사본을 `samples/`에 커밋하되, **무엇을 커밋할지**를 정함: 정형 CSV·청크·**Chroma 인덱스**(`samples/outputs`)는 커밋 → 클론 후 **키 없이 신호등·벡터 검색**이 동작(임베딩 재빌드에 필요한 API 키 불요). 원본 **PDF(약 44MB)는 제외**(`.gitignore: samples/data/*.pdf`) — 결과를 *보는 데는* 불필요하고, 0단계 재현이 필요한 사람만 공개 출처에서 받으면 됨(안내 `samples/data/README.md`). *트레이드오프*: Chroma 5.8MB를 히스토리에 넣는 대신 "검색까지 키 없이"를 지킨다(핵심 기능이라 값어치 있음). 커밋 규모 9.0MB/35파일. 작업 폴더(`/data/`·`/outputs/`)는 계속 gitignore라 재사용자 데이터와 충돌 0 — `scripts/bootstrap_samples.py`가 `samples/`→작업 폴더로 펼침(기존 파일 보호, `--force`로 초기화). *엔지니어 유보(생성물 커밋)는 '키 없이 검색' 요구가 상쇄.*

## 확정된 결정 (2026-07-03 · UX/UI 개편 + /ship 리뷰)

- **D16. "결과 먼저, 관리 나중" 3모드 IA** — 앱을 🚦 대시보드(정형 CSV 있으면 **랜딩**, 키·인덱스 불필요) · 💬 질의 · 🛠 데이터 준비(기존 1~4단계 게이트 스텝퍼 유지)로 재구조화. 신호등이 키 없이 동작하는 최종 성과물인데 6단계 맨 뒤에 있던 배치를 해소("클론 즉시 신호등"과 일치). *사용자 확정 선택.* D4(스텝퍼 유지)를 대체·발전.
- **D17. 검수 UX — 순차 검수 + 원문 페이지 미리보기** — 저장하면 자동으로 다음 미검수 행으로 가는 **순차 검수 모드**(st.dataframe 선택은 읽기 전용이라 표 기반 자동 이동 불가 → 포인터 방식), 검수 상세에 **원문 PDF 페이지 렌더**(extract_vision 재사용, PDF 없으면 폴백) — "사람이 원문 보고 확정"을 앱 안에서 완결. 상태 라디오 기본값을 변조성 '값 고침'에서 **'원래 값 맞음'으로**(안전 기본값). *사용자 확정 선택.*
- **D18. 하이브리드 게이트 안전 강화(/ship 레드팀·적대 리뷰 반영)** — ① **사람 우선 가드**: adjudicate 가 실행 중 사람이 검수한 행을 호출 전·쓰기 직전 이중 재확인으로 건너뜀(최신 기록 우선 규칙이 사람 판단을 뒤집지 못하게). ② **빈 값 agree 는 에스컬레이션**(결측을 llm_verified 로 매장 금지). ③ **비전 후보 기각 = `confirmed`(원래 값 유지)** — `skip` 기록은 chunking 이 행 자체를 인덱스에서 제외해 조용한 데이터 소실이었음. ④ 검수 UI 가 `llm_verified`를 fixed 와 동일 규칙으로 표시(색인과 화면의 값 일치). *미결로 남긴 것*: llm_verified 의 **사람 승인 단계**(Stage 3 후보) — [`PLAN.md`](./PLAN.md) '리뷰 이연 항목' ①.
- **D19. 모드 개명 + 사이드바/도움말 가독성** — 실행 중 앱 스크린샷 리뷰로 **'💬 질의(Q&A)' → '💬 AI에게 묻기'** 개명(D16의 '질의'를 대체 — "데이터 근거로 AI와 대화"가 본질). 모드 네비·화면 헤더('RAG' 전문용어 제거)·사이드바·캡션·E2E 일괄. 좁은 사이드바에서 단어가 중간에 잘리던 '다음 할 일'·'인덱스 정합'을 마크다운 hard break로 '무엇을(굵게)+어떻게' 두 줄 구성. *사용자 확정 선택(이름은 AskUserQuestion으로).*

---

## 당시 상태 (2026-06-26 사실 확인 — 이후 변화는 위 확정 결정·PLAN 참고)
- 인제스트 실제 완주 확인: 추출 190s · 표준화 50s · 라벨표준화 181s · 중복 2s · 플래그 127s · 검수큐 60s ≈ **약 10분**(2023 PDF 1개 기준).
- 검수 큐 289행(미검수 high 33). 라이브 인덱스 198청크(게이트 미통과 데이터로 구축됨 — 아래 D5).
- 동작은 하지만 (1) 인제스트가 느리고 (2) 각 단계가 무슨 작업인지/다음에 뭘 할지 안내가 부족하고 (3) 검수에서 "값 없는 행"을 사용자가 직접 찾아야 함.

---

> **처리 현황(2026-07-03 주석)** — 아래 D1~D6 은 2026-06-26 의 결정 요청 양식(당시 번호 체계)이다. 이후 실제 처리:
> **D1**=A 구현(스킵 캐시 `pipeline.is_fresh` + 강제 재실행 + E2E, refine 병렬화로 B 일부) · **D2**=A 구현(STEP_TODO 배너·다음 단계 버튼) · **D3**=A+B+C+D 구현(빈값 배너·필터·정렬 + **순차 검수 모드**(C) + 게이트 연동 '남은 검수' 패널(D)) · **D4**=A 로 운용 후 **D16(3모드 IA)으로 대체** · **D5**=C 실행(경고 표시 + 검수·정화 후 게이트 통과 재인덱싱 — 현재 라이브 인덱스 218청크는 게이트 통과 데이터) · **D6**=C 실행(`@slow` 실 LLM 테스트 + eval 골드 9케이스).

---

## D1. 인제스트 속도 최적화

**관찰**: 약 10분. 원인은 **표준화 이후 단계가 매번 전 연도를 LLM으로 재처리**(`standardize`가 `outputs/*.extracted.jsonl` 전체 글로빙)하기 때문. PDF 1개만 추가/수정해도 2023·2024·2025 전체를 다시 돌림.

**선택지**
- **A. 변경 없으면 단계 스킵(캐시)**: 입력 파일이 그대로면 산출물 재사용(타임스탬프/해시 비교). 재실행 시간 대폭 절감. "강제 재실행" 옵션 유지. → *중간 규모, 안전.*
- **B. LLM 배치 호출**: `extract`가 지금 블록당 1콜 → 배치로 묶음(`refine`/`flags`도). 속도↑, 정확도 동일. → *중간 규모.*
- **C. 증분 처리**: 바뀐 연도만 `refine`/`flags` 재실행. 표준화 사전이 누적식이라 까다롭고 위험. → *큰 변경, 비추천(지금은).*
- **D. 그대로 두기**: 가끔 도는 배치로 보고 최적화 안 함, 진행 표시만 유지. → *0 작업.*

**제 추천**: **A(스킵 캐시)** 먼저. 가장 효과 크고 안전. 반복 실행이 잦으면 **B**도. (C는 나중.)
**결정 시 할 일**: `pipeline.py`에 "입력이 최신이면 스킵" 로직 + 단계별 "스킵됨/재실행" 표시. (B 선택 시 extract 배치화.)
**검증**: 같은 데이터로 2회 실행 → 2회차가 LLM 단계 스킵으로 수초 내 완료. E2E로 "스킵" 표시 확인.

---

## D2. 각 단계 "무슨 작업인지" 설명 + "다음 행동" 안내 (UX)

**관찰**: 단계 상단에 그 단계가 무엇을/왜 하는지, 끝나면 다음에 뭘 눌러야 하는지 안내가 약함.

**선택지**
- **A. 전 단계 적용**: 각 단계 상단에 1~2줄 설명("이 단계는 X를 합니다 · 보통 Y분 · 끝나면 Z") + 완료 후 **"다음: N단계로 가기" 버튼**. → *작고 안전, 추천.*
- **B. 일부 단계만**.

**제 추천**: **A**. 저위험 UX 개선.
**결정 시 할 일**: 5개 단계에 설명 헤더 + 완료 시 다음-단계 이동 버튼. 사이드 "다음 할 일" 안내와 일관.
**검증**: Playwright로 각 단계 설명 텍스트·다음 버튼 노출 확인.

---

## D3. 검수 안내 — "값 없는 행"을 사용자가 일일이 찾지 않게 ★사용자 핵심 지적★

**관찰**: 검수 큐 289행에서 **기본값·정정값이 모두 없는(빈 값) 행**을 사용자가 직접 찾아야 함. 부적절.

**선택지**
- **A. 상단 배너 + 기본 필터**: "⚠️ 값이 비어 반드시 검수 필요: N행" 배너 + **"값 없는 행만 보기" 필터(기본 ON)**. 못 찾는 문제 해소.
- **B. 정렬**: 값 없는 행을 표 맨 위로.
- **C. 순차 안내**: "다음 미처리 행으로 →" 버튼으로 하나씩 이동.
- **D. 게이트 연동**: `validate.py`의 차단 항목(빈값·미확정 비전·미검수 high)을 검수 탭 상단 **"할 일 목록"**으로 띄우고, 클릭하면 그 행들만 필터. (게이트 ↔ 검수 UI 연결)

**제 추천**: **A + B**(배너+필터+정렬)로 즉시 해결, 여유 되면 **D**(게이트 연동)로 "무엇을 검수해야 인덱싱 가능한지" 한눈에. C는 선택.
**결정 시 할 일**: 검수 탭에 빈값 카운트 배너 + "값 없는 행만" 필터/정렬. (D 선택 시 게이트 항목→필터 버튼.)
**검증**: Playwright로 배너·필터 동작, 빈값 행이 먼저 보이는지 확인.

---

## D4. 흐름 범위 — 스텝퍼 유지? (이전부터 미결)

**배경**: 원래 작은 UX 요청을 제가 전면 스텝퍼로 키웠음. 다만 지금은 동작·완주 확인됨.

**선택지**
- **A. 현 스텝퍼 유지** + D2/D3로 안내 보강. → *추천(이미 동작, 흐름 명확).*
- **B. 최소안 축소**: 탭 구조 복귀 + 로깅/진행만. → *방금 만든 것 상당 폐기.*
- **C. 절충**: 스텝퍼 유지하되 인제스트 등 고급 기능 접기.

**제 추천**: **A**.

---

## D5. 인덱스–게이트 정합 (원칙 위반 해소, 백로그 A)

**관찰**: 라이브 인덱스(198청크)가 게이트 미통과 데이터(빈값·미확정 비전 38·미검수 high 33)로 구축됨 → "추측은 데이터 아님" 원칙을 실제로는 위반 중.

**선택지**
- **A. 검수로 확정 후 재인덱싱**: D3로 검수 → 게이트 통과 → 재인덱싱. *정공법(시간 듦).*
- **B. 경고만**: 앱에 "인덱스가 미확정 데이터 포함/게이트 미통과" 경고 + 신선도 표시. *빠른 임시.*
- **C. A+B**: 경고 먼저 띄우고, 검수 진행하며 점차 정합.

**제 추천**: **C** — 지금 경고 표시(정직성) + D3 검수로 점진 정합 후 재인덱싱.

---

## D6. 검증 깊이 (백로그 C)

**관찰**: E2E가 전부 `RAG_FAKE_LLM`이라 UI 배선만 증명. 실제 검색/답변/로그/인제스트 미검증.

**선택지**
- **A. 실제 LLM 테스트 1~2건(`@pytest.mark.slow`)** + 🩺 로그 패널 내용 단언 추가.
- **B. `eval/` 평가셋**(질문→기대 출처/항목)으로 rerank·프롬프트 회귀 정량 측정.
- **C. A+B.**

**제 추천**: **A 먼저**(저비용), 이후 **B**.

---

## 우선순위(제 제안)
1. **D3**(검수 안내 — 사용자 핵심 불편) +
2. **D2**(단계 설명/다음 행동) — 둘 다 저위험·즉효
3. **D5**(인덱스 정합: 우선 B 경고 표시)
4. **D1**(인제스트 최적화 A 스킵)
5. **D6**(실제 검증), 그 뒤 D4 확정·범위(D 백로그)

---

## 결정 양식 (이렇게 답해주시면 됩니다)
```
D1 = A         (또는 A+B / D)
D2 = A
D3 = A+B       (또는 +D)
D4 = A
D5 = C
D6 = A
우선순위 = 제안대로  (또는 직접 지정)
```
- 추가로 바꾸고 싶은 점이나, 위에 없는 항목이 있으면 자유롭게 적어주세요.
- 결정 주시면 **그 범위 안에서만** 단계별로 구현하고, 각 증분을 검증한 뒤 진행하겠습니다.
