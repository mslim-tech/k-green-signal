# rag/core/paths.py
# -----------------------------------------------------------------------------
# 산출물 디렉터리를 한 곳에서 정한다(테스트 격리용 env override 지원).
#   - 기본은 'outputs'. 환경변수 RAG_OUTPUT_DIR 가 있으면 그 경로를 쓴다.
#   - E2E 는 이 변수로 '임시 복사본'을 가리켜, 실제 outputs/(통합 데이터)를
#     절대 건드리지 않는다(인제스트 테스트가 파이프라인을 실제로 돌려도 안전).
# -----------------------------------------------------------------------------

import os
from pathlib import Path

OUTPUT_DIR = Path(os.environ.get("RAG_OUTPUT_DIR", "outputs"))
